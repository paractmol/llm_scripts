# LLM scripts

### git-commit.py

The script leveraging the use of gpt4all to generate Git commit messages.

```fish
> python3.11 git-commit.py
Commit message: Update README.md with new section on `git-commit.py`
Do you like the commit message? ([y]es/[n]o/[r]etry):
```
